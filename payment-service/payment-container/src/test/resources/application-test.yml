spring:
  datasource:
    url: jdbc:tc:postgresql:16-alpine:///payment_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    default-schema: public
