meta {
  name: Refund Payment
  type: http
  seq: 2
}

post {
  url: {{paymentUrl}}/api/v1/payments/refund
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "reservationId": "{{reservationId}}"
  }
}
